<%= @project.name %><br>

<h1>New Task</h1>

<div id="tasks_form">
	<%= render 'tasks/form' %>
</div>

<div id="tasks_list">
	<% if @tasks.present? %>
		<%= render partial: 'tasks/task_list', locals: {task_list: @tasks} %>
	<% end %>
</div>