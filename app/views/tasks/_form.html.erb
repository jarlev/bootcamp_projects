

<%= form_for Task.new do |f| %>
	<%= f.label :task_name %>
	<%= f.text_area :task_name %><br>
	<%= f.label :task_description %>
	<%= f.text_area :task_description %><br>
	<%= f.label :priority %>
	<%= f.number_field :priority, :in => 1..10 %>
	<br>
	<%= f.submit "Create task" %><br>
<% end %>